# food_nutrition

## to setup the app
cd food_nutrition
npm install

cd src/api/node_printer && npm i --save-dev nodemon

## run the app
cd food_nutrition
npm run dev

cd src/api/node_printer && npm run start
